import path from 'path';

export const ROOT_PATH = path.resolve(__dirname, '..');
export const DIST_PATH = path.resolve(ROOT_PATH, 'dist');
export const TEMPLATE_PATH = path.resolve(ROOT_PATH, 'assets', 'template.ejs');
